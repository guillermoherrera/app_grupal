import 'package:flutter/material.dart';

class RenovacionesGrupoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
      body: Container(child: Center(child: Text('data'),),)
    );
  }
}